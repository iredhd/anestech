import React from 'react';

import { Template } from '../../components';
import { SearchSection } from './components';

const Home = () => (
  <Template>
    <SearchSection />
  </Template>
);

export default Home;
